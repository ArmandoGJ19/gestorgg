instalar antes de ejecutar ese desmadre

pip install fastapi motor pymongo decouple, uvicorn
pip install python-jose
pip install bcrypt
pip3 install cloudinary

run uvicorn main:app --reload

-validaciones
pip install pydantic[email]
